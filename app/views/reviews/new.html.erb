<h1> Event Reviews#new</h1>
<%= simple_form_for [@booking, @review] do |f| %>
  <%= f.input :comment %>
  <%= f.input :rating, collection: 0..5, prompt: "Select a rating"%>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>